<h1>Editing book</h1>

<%= form_with model: @book, url: update_book_path(@book.id), method: :patch do |f| %>s do |f| %>

<h4>Title</h4>
<%= f.text_field :title %>

<h4>Option</h4>
<%= f.text_area :body %>

<br><%= f.submit 'Create Book' %></br>
<% end %>

<td><%= link_to "show", book_path %></td>

<th>|</th>

<%= link_to 'back', books_path %>